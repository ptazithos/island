from SCons.Script import Environment

env = Environment(CC='clang')
program = env.Program('main.c')

env.AddPostAction(program, 
"@echo '-----------------';"
"./build/main;" \
"echo '-----------------';")